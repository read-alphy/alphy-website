/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.ytimg.com', 'flagcdn.com', 'alphy.blob.core.windows.net'],
    },
  };
  
  export default nextConfig;

   